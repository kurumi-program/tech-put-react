export type UserProfile = {
  id: string;
  userId: string;
  userName: string;
  userUserName: string;
  email: string;
  bio: string;
  avatarUrl: string | null;
  githubUrl: string;
  postCount: number;
};
